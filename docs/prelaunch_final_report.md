# 【PRELAUNCH-FINAL-MP 苏程记录第一次上架候选版本最终验收报告】

## 一、开始状态

开始 HEAD / `origin/main` 均为 `af259e8ecffe96b47457a2f44c3a13e41954943f`，ahead/behind=0/0，分支 `main`，工作区/暂存区/未跟踪文件为空，无 index.lock。两次状态、HEAD 和 335 个主要文件修改时间对比无变化；仅发现本任务自身进程持有仓库 cwd，没有并发写入。

## 二、正式身份

正式名称“苏程记录”，AppID `wxc2a2a94f767438dd`，`compileType=miniprogram`，`miniprogramRoot=./`，基础库配置 3.7.12。私有配置被 Git 忽略、无 AppID；本机项目名已备份后改为“苏程记录”，不提交。旧 AppID 仅存在历史文档/测试断言，当前配置与运行页面为 0；旧名称当前运行命中为 0。

## 三、自动基线

基线 103/103 TEST-ID、85/85 历史脚本通过。最终新增 PRELAUNCH 门禁后为 103/103 TEST-ID、86/86 历史脚本通过；RC11-1、RC11-2、两个 smoke、全仓 JS、38 个 JSON、上传包、生成规则、身份、禁用能力和数据哈希全部 PASS。

## 四、开发者工具

应用路径 `/Applications/wechatwebdevtools.app`，RC 2.02.2607171（bundle 36.6.0）。正确仓库路径与对正式 AppID 的属性请求已由日志确认；但当前登录用户不是该 AppID 的开发者，模拟器创建失败。普通编译、Problems、Console、首页和最终复验均未形成结果；不能声称通过。CLI 服务端口关闭，未改安全设置、未登录/扫码/预览/上传。

## 五、五个主导航

首页、学校库、成绩、目标规划、我的的路由、入口、单一数据流和 onShow 刷新矩阵由自动门禁通过；开发者工具五 Tab 点击和通用状态未运行。

## 六、学校用户路径

搜索、组合筛选、收藏、候选、标签、备注、目标学校、主要目标和最多三校对比的自动服务/页面契约通过；开发者工具真实点击未运行。

## 七、成绩用户路径

模板、分值方案、不同满分、固定考试案例、得分率、1/2/3/5/9/10/11 条趋势、坐标及 740 资格自动通过；开发者工具真实录入未运行。

## 八、学习闭环

复盘、失分原因、错题、学习任务、周计划、阶段目标、阶段复盘的事务、引用、隔离、幂等和恢复自动通过；真实页面路径未运行。

## 九、历史分差参考

固定公式、冲刺/目标/保底边界、每组 5 所、合格参考考试、情景成绩和免责声明自动通过；用户可见禁用预测/建议文案扫描通过。

## 十、多档案

双档案实体隔离、共享收藏策略、删除与恢复由隔离测试通过；没有在真实模拟器创建档案 A/B。

## 十一、数据安全

由于模拟器未创建，P5 写入门禁未能建立真实恢复点、Backup v3 和 before checksum，所以没有写入任何临时用户数据。before/after checksum 均为空，不能声称恢复一致；MemoryStorage/FakeFaultInjector 下的安全链路全部通过。

## 十二、备份、恢复点和报告

导出、导入、合并/覆盖、v2/v3、恢复点 v1/v2、四类 text/JSON 报告和 FakeFileShareAdapter 自动通过；真实微信文件发送与模拟器恢复未运行。

## 十三、异常恢复

12 类启动状态、自动/用户处理、清除不复活、dataRevision、锁、幂等和恢复证据均在隔离测试中通过；未对真实用户 Storage 注入故障。

## 十四、多尺寸

320/375/390/414/430 的趋势坐标与标签同 X 契约通过，静态可访问性通过；六种尺寸全页面人工验收数为 0，iPad 未运行。

## 十五、缺陷

共 2 项：P1=1、P2=1，均已修复并由最终全量回归验证。P0=0、P3=0。账号权限是平台阻断，不算代码缺陷。

## 十六、合规和上传包

产品、隐私、主动分享和禁用能力自动检查通过。包共 127 个文件、880,708 字节，最大文件 `utils/rc9-storage.js` 121,661 字节，开发资料 0、必需文件缺失 0；未上传体验版。

## 十七、最终回归

103/103 TEST-ID、86/86 verify、两个 smoke、JS/JSON、包、身份、禁用能力、产品规则、正式数据和哈希全部通过。正式数据 55/103/43/146，2027=0，中考上限 740，三份 raw/semantic SHA-256 未变。用户 checksum 因零模拟器写入而未生成。

## 十八、人工剩余事项

只保留授权开发者账号、普通编译/Problems/Console、完整页面与尺寸、真实安全 checksum、真机、真实微信文件发送、体验版上传和审核。

## 十九、Git

开始保护分支 `backup/prelaunch-final-start-20260801-af259e8` 已普通推送。已普通推送提交：`c73ff60`、`34fc36e`、`40c212f`、`b8bdb1c`；最终证据提交和最终保护分支由后续 Git 门禁记录。仓库外备份：`/Users/tom/WorkData/05_Backups/suzhou_highschool_miniprogram/PRELAUNCH_FINAL_MP_20260801_224816`。回滚仅用普通 `git revert <sha>`。

## 二十、最终状态

- `V1_CODE_FREEZE_READY`：保持确认。
- `PRE_RELEASE_UX_FREEZE_CONFIRMED`：否。
- `FIRST_SUBMISSION_CODE_READY`：否。
- `FIRST_SUBMISSION_CODE_READY_PENDING_REAL_DEVICE_ACCEPTANCE`：不适用，因为开发者工具编译与 UX 验收尚未完成。
- 已知代码阻断：无；平台阻断：当前微信账号不是正式 AppID 的开发者。
- 是否可进入上传体验版和提交审核人工阶段：否。先完成授权账号下的 P3—P5/P19 与全页面、多尺寸、数据恢复验收；再由用户授权上传和提交审核。
