# RC11-2 版本冲突规则

成绩、复盘、失分原因、学习任务、阶段目标、目标学校状态和学生档案规范化后都有 `version`，缺失时为 1。成功编辑时版本加 1。

页面加载的数据携带 version；正式 save 方法把 `expectedVersion`（或传入记录的 version）与当前正式记录比较。不一致返回 `VERSION_CONFLICT` 和“这条内容已在其他页面更新，请重新载入后再修改”，不覆盖、不合并文字。恢复和导入按批量事务/恢复点处理，不以页面单条版本直接覆盖。
