# RC10 小程序数据质量与学习目标深化报告

## 状态与备份

- 开始 HEAD / 本地 `origin/main`：`14edfd490ab3b2607d4a242dcf58ffc67a8be3c9`
- 仓库外完整备份：`/Users/tom/WorkData/05_Backups/suzhou_highschool_miniprogram/RC10_FULL_20260730_225536`
- 正式名称：苏程记录
- AppID：`wx17e903f81714736f`
- 版本：`2.0.0`
- storageSchemaVersion：4
- backupFormatVersion：2

## 实现

- `utils/storage.js` 已收口为唯一版本化入口，旧兼容转换只留在迁移代码。
- `utils/rc9-storage.js` 增加事务日志、旧值快照、逐键回读、失败回滚和启动恢复。
- `utils/rc9-models.js` 增加 `scoreReviews`、`scoreLossReasons`、`learningTasks`、`scenarioSettings`、`recentHistory`。
- 目标规划的推荐分段增加当前成绩、下一阶段目标、中考目标三种情景；分差规则仍为冲刺 -30..-1、目标 0..15、保底 >15。
- 复盘分段支持 10 类失分原因、学科、说明、改进措施、增删和个人记录统计。
- 每条失分原因可创建统一学习任务，包含来源考试/复盘/原因、阶段目标、截止日期、每周次数和四种状态；重复来源默认阻断。
- 学习目标分段同时展示阶段目标、学习任务、本周/完成/未完成/即将到期/暂停摘要。
- 目标学校轨迹继续复用稳定成绩排序；学校详情增加真实年份逐项目趋势、当前/阶段/中考目标分差。
- 学校对比要求至少两所，支持前移/后移、当前/阶段/中考分差、官方来源和用户备注；不提供主观评分或结论。
- 最近历史覆盖浏览学校、筛选、对比、考试、目标、档案及成绩/目标分段，并可单独全部清除。
- RC10 备份使用统一顶层字段，仍保留档案容器以保存未知字段；支持 RC9 v1 读取、RC10 v2 校验、合并、覆盖与失败不改现有数据。
- 数据管理增加 23 类只读检测、安全修复、修复前快照和恢复；无法判断归属、分数或学校的项不自动修复。
- 动态帮助按状态显示、逐项版本化关闭，并可在帮助中心重播。
- `docs/rc10_school_data_quality_matrix.md` 覆盖 55 所学校。
- `docs/annual_data_update_2027_workflow.md` 与候选校验工具已建立；正式 2027 分数线仍为 0。

## 性能与异常分支

`scripts/verify_rc10_performance.js` 使用内存与模拟本地存储覆盖 100/500 条成绩、3×100 与 5×200 多档案、100 个阶段目标、500 个学习任务、100 条复盘、500 条失分原因、100 次筛选和 50 次档案切换。一次完整专项实测：排序 14.79ms、趋势 2.59ms、三情景 1.37ms、多档案数据 0.78ms、目标/任务/复盘/原因 3.12ms、100 次筛选 44.00ms、50 次档案切换 47.67ms。测试数据只在脚本内或模拟存储生成，不写入正式数据。

事务专项模拟 profileData 写入失败，确认新增记录未出现、旧状态逐字段恢复、事务日志清理；另模拟异常退出遗留日志，确认下一次初始化先恢复旧值。

按 `project.config.json` 上传排除规则计算，正式运行文件为 92 个、614,875 bytes（约 0.59MiB）；DevTools 实际上传压缩包大小仍以人工预览/上传界面为准。

## 自动验证

- RC10 新增 18 个专项脚本，由 `scripts/verify_rc10_full.js` 串联。
- RC9 14 个专项与 RC8 竖直对齐测试继续通过。
- `scripts/verify_rc10_cross_platform_backup.js` 已通过真实双向桥接：小程序解析 Flutter 导出，Flutter `previewLocalBackup` 解析小程序导出；桥接使用 `flutter test`，没有以普通 Dart 模拟 Flutter 插件环境。
- 正式数据数量：55 / 103 / 43 / 146。
- 正式数据 SHA-256 与 RC9 基线一致。
- 当前无登录、后台、云开发、AI、用户上传或正式 2027 数据。
- 全部历史 `verify_*.js`、两项 smoke、全仓 `node --check`、`git diff --check`、双端一致性 16/16 和 Flutter 本地链路 6/6 已通过。

## 微信开发者工具

- 使用微信开发者工具 RC `2.02.2607171` 打开真实项目路径并实际执行“普通编译”。
- 初次 Problems 的 3 个错误来自 WXML 动态内联 style 被 CSS 语言服务拆分解析；将同一几何值预计算为 `labelStyle` 后，RC8 同 x 专项、RC9、RC10 和页面 smoke 全部重过。
- 最终 Problems 为 0，模拟器重新加载“苏程记录”，Console 未显示可访问的运行时错误。
- 只读切换检查：首页、学校库、成绩、目标规划、我的。成绩页可见记录/趋势/复盘，目标页可见三情景，个人页可见动态帮助、备份与数据管理入口。
- 未执行：学校详情、对比、失分原因编辑、创建任务、导入/导出、大字体和键盘的完整交互验收；未执行手机预览、体验版上传或审核。

最终 push 与双仓远端状态在双端总报告中记录。
