# MP10 AppID 与正确项目导入指南

## 当前状态

- 已写入 AppID：`wx17e903f81714736f`
- 正确项目路径：`/Users/tom/Dev/suzhou_highschool_miniprogram`
- 后端服务选择：不使用云服务
- 不填写 AppSecret
- 不启用云开发
- 不写入 token、密码或其他密钥

## 错误项目现象

如果微信开发者工具左侧显示 `cloudfunctions`、`miniprogram` 子目录，并且模拟器显示“快速了解云开发”，说明当前打开的是云开发模板项目，不是正式项目。

不要导入以下路径：

- `/Users/tom/Dev`
- `/Users/tom/Dev/suzhou_highschool_miniprogram/miniprogram`
- 任何包含 `cloudfunctions` 的云开发模板项目

## 正确项目导入后应看到

左侧文件树应在项目根目录直接看到：

- `app.js`
- `app.json`
- `pages`
- `data`
- `utils`
- `config`
- `docs`

## 关闭错误项目

1. 在微信开发者工具顶部菜单选择项目列表或项目管理入口。
2. 关闭当前显示“快速了解云开发”的项目窗口。
3. 不要在错误项目内移动文件、复制正式项目或新建 `cloudfunctions`。

## 重新导入正确项目

1. 打开微信开发者工具。
2. 选择导入项目。
3. 项目目录选择 `/Users/tom/Dev/suzhou_highschool_miniprogram`。
4. AppID 确认为 `wx17e903f81714736f`。
5. 后端服务选择“不使用云服务”。
6. 完成导入后，确认左侧直接显示 `app.js`、`app.json`、`pages`、`data`、`utils`。

## 编译与报错查看

1. 点击微信开发者工具顶部“编译”。
2. 如果模拟器页面异常，打开 Console。
3. 只关注红色报错全文。
4. 如果需要 Codex 继续处理，请提供 Console 红色报错全文和当前打开的项目路径截图。

## 服务端口关闭时的处理

如果 CLI 提示服务端口关闭，在微信开发者工具中打开：

`设置 / 偏好设置 -> 安全 -> 开启服务端口`

开启后可以重新运行 CLI 打开项目命令，或手动重新导入正确项目。

## 预览

1. 编译通过后点击“预览”。
2. 使用真实微信扫码。
3. 检查首页、学校库、学校详情、收藏、学习目标、数据说明、隐私说明和我的页。
4. 当前版本只使用本地数据和本地存储，不接登录、定位、支付、广告、云开发或后台请求。

## 上传开发版本

默认不要自动上传开发版本。只有同时满足以下条件后，才由用户在微信开发者工具中手动上传：

- 项目路径确认正确
- AppID 确认为 `wx17e903f81714736f`
- 编译通过
- 真机预览通过
- 用户已登录微信开发者工具
- 用户明确决定上传开发版本

上传信息建议：

- 版本号：`1.4.0`
- 版本描述：`MP6 最终收口版：展示苏州高中公开基础信息、2025 官方历史分数线、本地收藏和学习目标记录。`

不要填写 AppSecret，不要启用云开发，不要提交审核，除非用户已经完成真机检查并明确决定继续。
