# V1 学校个人状态规格

- `schoolUserStates` 按当前档案保存候选状态、标签、备注与自定义顺序；“当前目标”和“主要目标”继续从目标记录推导，不重复存储。
- 候选状态为 `none`、`exploring`、`focused`、`not_considering`。
- 同一筛选类别内为 OR，不同类别之间为 AND；支持候选状态、标签、有备注、最近浏览、最近对比、当前目标和主要目标。
- 标签和备注不进入历史分差计算；学校对比只展示用户维度，不产生综合评分、推荐指数或录取概率。
