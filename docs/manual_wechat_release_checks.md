# FCP 微信发布前人工验收清单

- 正式产品：苏程记录
- 正式 AppID：`wxc2a2a94f767438dd`
- 项目路径：`/Users/tom/Dev/suzhou_highschool_miniprogram`
- 当前范围：10 个正式页面、5 个 Tab，不包含历史已移除功能。

本清单只用于微信开发者工具、真机、体验版和微信公众平台的人工验收。未实际执行的项目不得标记为通过；自动门禁、模拟器、当前 HEAD 真机、体验版上传和审核是五层独立证据。

## 1. 开始前

| 检查项 | 操作 | 通过标准 |
|---|---|---|
| 真实项目 | 从上述路径导入微信开发者工具 | 项目名称正确，AppID 精确为 `wxc2a2a94f767438dd` |
| 构建保护 | 打开项目详情 | `ignoreDevUnusedFiles=false`，`uploadWithSourceMap=false` |
| 清理现场 | 清空 Console 和 Problems，点击普通“编译” | Problems = 0；Console 无业务错误 |
| 历史 P0 | 查看编译与运行输出 | 不出现 `module ... is not defined`、`wx://not-found`、`MaxCodeSize`、`MinTabbarCount` |

## 2. 正式页面与五个 Tab

依次打开：首页、学校库、成绩、目标、我的。再从正式入口进入学校详情、档案管理、数据备份与恢复、使用说明、隐私说明。

通过标准：

- 10 页都能到达，无白屏、无错路由、无 `undefined`、`NaN` 或 `Infinity`。
- iPhone 8 Plus 等小屏上无主要文字截断、TabBar 遮挡、横向滚动或按钮无法点击。
- 正式 UI 不出现自动推荐、目标等级、录取概率、单科、排名、考试模板、报告、用户可见技术维护信息等已移除功能。

## 3. 状态矩阵

在可回滚的测试档案中验收，测试后恢复原状态：

| 状态 | 通过标准 |
|---|---|
| 无成绩 + 无目标 | 首页、成绩、目标均显示正式空状态 |
| 有成绩 + 无目标 | 首页和趋势显示成绩，目标仍为空状态 |
| 无成绩 + 有目标 | 目标学校可见，当前成绩和数字差值不伪造数据 |
| 有成绩 + 有目标 | 仅显示 `当前总分 - 历史公开参考分` 的数字差值 |
| 单档案 / 双档案 | 切换后成绩与目标完全隔离，删除 A 不影响 B |
| 0 分 / 740 分 | 两个边界均可保存并正确绘制 |
| 长学校名 | 详情、目标和列表可读，不遮挡主要操作 |
| 无历史分 / 多条历史分 | 分别显示空状态和完整多条记录，不错误合并 |

## 4. 核心交互

| 范围 | 操作 | 通过标准 |
|---|---|---|
| 学校 | 搜索正式名称、别名“南航苏附”、空格、无结果；组合区域/类型/年份/分数筛选 | 结果准确，清空可恢复，最低分大于最高分时有可理解结果 |
| 学校详情 | 打开有/无历史分学校；用失效 schoolId 打开；复制名称、地址、地图搜索词、官网和公开来源 | 记录年份倒序；失效 ID 有空状态和返回按钮；复制内容精确 |
| 成绩 | 新增、编辑、删除；测试 0、1、323、739、740、741、负数、小数、字母、空值、异常日期 | 0—740 整数边界正确，其他非法值拒绝；保存后只产生名称、日期、总分等首发字段 |
| 趋势 | 分别准备 0、1、2、10、11、100 条成绩 | 最多显示最近 10 条，点、考试名称和日期共用准确横坐标 |
| 目标 | 加入、重复加入、打开详情、移除 | 不重复；无历参考时不伪造数字；不产生等级、判断、概率或推荐 |
| 档案 | 新建、改名、切换、删除，并验证 10/11 个边界 | 最多 10 个，昵称 1—20 字符，第 11 个明确拒绝且旧数据不变 |
| 客服 | 在使用说明复制邮箱和微信号 | 分别精确复制 `3341251927@qq.com` 和 `shsz1610` |
| 备份 | 打开备份页；取消“从聊天中选择备份”；打开导出入口 | 取消不报错不改数据；导出只在用户主动确认后打开文件发送界面 |

## 5. 隐私与网络边界

- 微信隐私保护指引中的实际接口应为 Clipboard 和 MessageFile。
- Clipboard 仅由用户点击复制触发，不得存在 `wx.getClipboardData`。
- MessageFile 仅由用户主动选择苏程记录备份触发；不后台扫描、不自动上传。
- 导出备份只在用户主动确认后调用微信文件发送界面。
- 不应出现登录、头像昵称、手机号、定位、支付、广告、云开发、统计 SDK、远程成绩上传或业务 `wx.request`。

## 6. 正式数据与上传包

| 检查项 | 通过标准 |
|---|---|
| 学校 | 55 所，schoolId 唯一 |
| 2025 分数线 | 103 条 |
| 2026 分数线 | 43 条 |
| 分数线总数 | 146 条，`>740 = 0` |
| 数据保护 | 三份正式数文件 SHA-256 与本轮开始前一致 |
| 上传包 | docs、scripts、本地报告和开发文件被 `packOptions.ignore` 排除；记录包大小、文件数和最大文件 |

## 7. 分层结论

| 层级 | 状态 | 必须记录的证据 |
|---|---|---|
| 自动门禁 | 待执行 | 命令、PASS/FAIL、失败原文 |
| DevTools 普通编译与模拟器 | 待执行 | Problems、Console、五 Tab、状态矩阵和核心交互 |
| 当前 HEAD 真机 | 待执行 | 机型、系统/微信/基础库版本、连接状态、五 Tab 和 Console |
| 体验版上传 | 待执行 | 版本号、包大小、上传日志 |
| 审核/发布 | 待执行 | 平台状态、审核原文或发布记录 |

只有某一层的真实验收完成后，才能将该层改为 PASS。任何登录、连接、上传或审核阻断都应保留现场和原始错误，不通过修改正式数据或恢复已移除功能来规避。
