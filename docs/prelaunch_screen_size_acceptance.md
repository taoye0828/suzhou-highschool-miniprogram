# PRELAUNCH-FINAL-MP 多尺寸验收

## 自动尺寸契约

趋势坐标专项测试在逻辑 CSS 坐标中通过：

| 宽度 | 1/2/3/5/10 条坐标 | 标签同 X | 结果 |
| --- | --- | --- | --- |
| 320 | 已验证 | 是 | PASS（仅趋势契约） |
| 375 | 已验证 | 是 | PASS（仅趋势契约） |
| 390 | 已验证 | 是 | PASS（仅趋势契约） |
| 414 | 已验证 | 是 | PASS（仅趋势契约） |
| 430 | 已验证 | 是 | PASS（仅趋势契约） |

同时通过 RC10 静态可访问性、五 Tab 导航与刷新矩阵门禁。

## 未完成的真实布局验收

账号权限阻止模拟器创建，因此 320、375、390、414、430 和 iPad 的全页面截图、横向溢出、键盘、安全区、弹窗、长文本、横屏与大字号基本可用性均未完成。完整通过尺寸数为 0；iPad 自动与人工页面验收均未运行。

这些项目保留为授权账号恢复后的开发者工具人工验收，不能写成代码已通过。
