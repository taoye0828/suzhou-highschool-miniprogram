# V1 报告与分享规格

- 报告类型：成绩阶段纯文本、成绩阶段 JSON、目标学校纯文本、目标学校 JSON；不生成 PDF。
- 报告只读取当前档案，并在生成时创建不可变快照。
- 报告文件只在本机生成；用户主动点击发送后，页面先显示数据范围和隐私提醒，再调用微信系统文件发送能力。
- 用户取消不记录为成功；发送失败不修改用户数据，可从同一页面重试。
- 目标学校报告包含统一历史分差边界声明和“历史公开数据整理，仅供目标规划参考。”。
